# Model Files

This directory stores model files for psy_supabase.

- Model files should be downloaded separately
- See documentation for how to obtain and place models
- Do not commit large model files to the repository

## Directory Structure
- `embedding/`: Embedding models
- `llm/`: Language model files
- `tokenizers/`: Tokenizer configurations
